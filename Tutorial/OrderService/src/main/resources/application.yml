spring:
  application:
    name: order-service
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest

server:
  port: 8081

rabbitmq:
  exchange: orders.exchange
  queue: orders.processing
  routing-key: order.created